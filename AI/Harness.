# AI Agent Harness (工程化脚手架与治理) 学习笔记

## 1. 什么是Harness？

这是一个概念 指的是：围绕AGENT 构建的的约束 反馈 控制的系统，使得AI能够在人类约定的环境里面持续的可靠的输出

> **【专家扩充】**：Harness 本质上就是 AI 的“脚手架”与“护栏”。不要指望 AI 靠自觉写出好代码，必须用工程化的手段去限制它的发挥空间。

核心在于 我们需要优化的不再是ai模型本身 而是模型运行的环境
人类不再干预ai的动作 而是构建一套系统进行引导约束

## 2. 为什么需要Harness

AGENTs有很多这样的问题

1. 面对一个任务的时候 模型会在一个对话里面吧这个任务完成 如果仓库很大 那么上下文窗口直接爆了
   > 这就是典型的“上下文爆炸（Context Overflow）”，必须引入懒加载和地图式索引机制。
2. 这个功能还只写了一半
   > 状态丢失问题，缺乏持久化的任务进度管理。
3. 如果另一个Agent来继续做 他还不知道上一个干了啥
   > Agent 之间的上下文隔离，需要建立标准化的 Handover（交接）协议。
4. 没有测试 可能代码有问题
   > 没有自动化测试的 AI 代码就是一颗定时炸弹。

这是一种不能通过模型的质量改善的问题

## 3. 信任债务

ai的成果可能会累积信任债务 就比如一个当下能够解决问题的方案 长远来看是有问题的 这样 每次生成代码都会积累问题 当问题爆发的时候 就只能逆向工程了

> 这叫“技术债（Technical Debt）”！AI 是极度短视的，为了眼前的目标会不择手段（比如写死硬编码）。如果不加约束，它产出的屎山代码比人类写的还要难重构。

## 4. 传统工程的问题

我们现在的工程 多是基于ai编写代码 人工review 来保证质量
但是ai的速度很快 人类的review速度是跟不上ai的编写速度的
并且这样的话项目的架构的演变也会变得很快 人工维护的方式也会变得不可行
人工维护的文档也会很快过时废弃
这就是Haness需要解决的

> **【专家扩充】**：这就是所谓的“人机速度不匹配”。必须用 AI 来打败 AI，引入“审查者 Agent”实现自动化的 Code Review 和文档更新。

## 5. 怎么Harness

实现可以分成三部分

1. **上下文工程**：在代码里面维护的知识库 让Agent能够动态的访问上下文（浏览器）并且这个知识库还要是地图式的 这样在能够提供信息的同时保证上下文窗口不会爆掉
   > 像 `agents.md` 或者 `.cursorrules` 就是地图的入口。
2. **架构约束**：需要使用明确的lint脚本 结构化测试 强制约束 并且采用层级依赖模型 例如 给代码分成 基建层面 配置层面 仓库层面 服务层面 运行层面 UI层面 这样层层依赖 禁止逆向 如果逆向直接CI失败
   > **【专家扩充】**：单向依赖（Unidirectional Dependency）是防止架构腐化的唯一物理手段！
3. **垃圾回收**：在后台定期运行  在后台定期运行一个Agent 扫面代码和文档之间的不一致性  约束违规的代码 以此减少墒增
   > **【专家扩充】**：这等同于自动化重构，定时清理代码库里的废弃代码和过期注释。

## 6. 迭代的理念

我们把AGENT的失败视作一个信号：因为Agent缺少了什么才会导致问题？
每次出现问题就进行一次迭代 并且进行功能验证

> **【专家扩充】**：不要抱怨 AI 笨，要反思你的环境是不是没提供足够的信息或工具！失败是优化 Harness 规则的最佳养料。

## 7. 克服失忆

Agent的上下文窗口不可以跨对话 那么注定了不能自带记忆 我们只能把记忆保存在代码仓库里面
那么在Agent下次执行让任务的时候 对于上次执行了什么人任务 成功还是失败 是完全不知道的
为了保存下记忆 我们可以这样：

1. 一个任务就是一个测试用例 禁止ai修改 删除 测试用例 只有测试用例通过 任务才是成功的
   > **【专家扩充】**：测试驱动开发（TDD）不仅是验证手段，更是业务逻辑的硬性记忆！
2. 强制一次只能执行一个任务
   > **【专家扩充】**：原子化操作，防止 Agent 思维发散和“精神分裂”。
3. 保存一个进度文件（比如openspec会进行归档） 那么修改一份代码的时候就能去看一下现状 或者是task的执行情况
   > **【专家扩充】**：这就是工程上的状态机（State Machine）持久化。

***

*(注：以下保留原笔记序号并进行模块化整理)*

## 进阶实践篇

### 1 Trace Analyzer Skill：用 Agent 优化 Agent 的 Harness

LangChain 最具创新性的贡献是 Trace Analyzer Skill——一套元层面的自动化工具，其工作流程如下：

1. 从 LangSmith 获取上一轮运行的追踪数据。（这个相当于大模型的dev tool）
   > **【专家扩充】**：获取 Agent 每一步思考和行动的“黑匣子”日志。
2. 并行启动多个错误分析 Agent，各自诊断失败原因。
3. 主 Agent 综合所有发现，提出对 Harness 的改进建议。
4. 对 Harness 进行针对性的修改，然后进入下一轮迭代。
   这个流程类似于机器学习中的 Boosting——每一轮都聚焦于上一轮的错误，进行迭代式改进。

### 7. 大模型钩子

在我们提交代码之前 会执行钩子函数 运行eslint等工具 大模型钩子就是使用lc加上一个钩子 在Agent宣告完成之前 强制拦截 运行测试 ，并且对过程的问题进行修复

> **【专家扩充】**：这就类似于 Git 的 `pre-commit hook`，物理切断劣质代码流入主干的路径，并且赋予 AI “闭环自愈”的能力。

### 8. 上下文注入

在Agent启动的时候注入必要的环境信息
这个可以使用Agent.md实现

> **【专家扩充】**：启动前就告诉它当前是什么项目、用什么技术栈、有什么红线绝对不能碰。

### 9. 动态的调整推理等级

在规划 和验证阶段将会使用最高的等级 执行阶段则会较低

> **【专家扩充】**：好钢用在刀刃上。让昂贵的高智商模型（如 o1-preview）做架构设计，让便宜的模型（如 GPT-4o-mini）做苦力写代码。

### 10. 五层核心组件

我们可以把Harness原则分层：

1. **文档与上下文**：agents.md是一个地图 不是一个百科全书 ，太多信息进入上下文窗口反而影响效果 而且太丰富的文档过时的很快 过时的信息将会影响效果 这个文档应该是有agent自动化的进行维护的
2. **架构约束**：层级依赖规则写成linter规则 这样agent在提交的时候就能够获得反馈 而不是占用上下文信息 也不需要人类介入
3. **ai观测**：  Chrome DevTools Protocol，让 Agent 能像人一样操作浏览器。 提供ai进入数据观测平台的能力 执行可以量化的任务   - 进行截图验证——使用 Puppeteer 等工具操作应用，然后截图与预期进行对比。
   > **【专家扩充】**：即无头浏览器自动化与视觉回归测试（Visual Regression Testing）。
4. **持续的清理**：在后台检查不符合预期的代码 然后把这些代码修改重构
5. **自动的cr**： 使用另一个agent对代码进行review
6. **CI/CD 流水线集成** —— 以上所有组件，必须在无人干预的自动化流水线中流转。

<br />

Q： 用于维护地图式上下文的方案：

1. agent.md:当ai需要修改某个文件夹下的文件的时候 将会阅读这个文件中的agent.md文件来获取上下文 
2. skill :ai会通过skill的desp来获取每个skill的作用 并且分析当前步骤是否有可用的skill 如果有就会把这个skill加载进上下文窗口
3. RAG : ai使用向量话检索的能力可以在目录中检索和目标相关的片段 并且返回片段 而不需要遍历所有数据
4. 基于lsp的代码导航 lsp是ide底层用于代码高亮 自动补全 跳转的协议 ，ai使用这个协议 就也可以获得代码之间的引用关系 建立关系地图
5. graph rag： RAG依赖的是相似的语义在向量空间的近距离，那么对于语义不类似的就没有作用了，而知识图谱增强可以。也就是说搜索增强生成的目的原本只在搜索 向量知识实现搜索的手段 而使用图的话检索的标准就从语义相关性 变成了逻辑相关性
6. 树状层级摘要: 提前对每个文件/文件夹进行总结 ai在查看仓库的时候 会从根目录的总结开始


Q：ai怎么查验自己的代码结果？

赋予ai自己查看使用浏览器的能力即可
要·

